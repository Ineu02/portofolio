# Static assets

Add the following files here:

- `og-image.png` (1200×630) — social sharing preview referenced in `src/app/layout.tsx`.

Anything placed in this folder is served from the site root (e.g. `/og-image.png`).
